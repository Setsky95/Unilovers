import "../UpperFooter.css";

const SuscriptionTitle = () => {
    return (
        <h2 className="suscription-title-text">
            Subscribe to our newsletter
        </h2>
    );
};

export default SuscriptionTitle;